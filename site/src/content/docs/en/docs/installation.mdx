---
title: Installation
description: How to install the Timeline DSL CLI.
---

Timeline DSL is used as a CLI tool. This guide assumes installation via Homebrew as the primary method.

```sh
brew tap keroway/tap
brew install tdsl
```

After installation, verify that the CLI runs correctly.

```sh
tdsl --help
```

## Requirements

- macOS or Linux (via Homebrew)
- Windows (manual binary installation)
- Git

For installation methods other than Homebrew (Linux one-liner, Windows binary, cargo), see [Homebrew and Other Installation Methods](/en/docs/homebrew/).

## VS Code Extension

Since v1.0.0, a VS Code extension providing syntax highlighting is available. It has been published on the Marketplace since v1.2.0, and snippets are available from v1.4.0 onwards.

### Installation

Search for `Timeline DSL` in the VS Code Extensions panel (`Ctrl+Shift+X` / `Cmd+Shift+X`) and install it. Alternatively, run the following command.

```sh
code --install-extension keroway.timeline-dsl
```

### Available snippets

| Prefix     | Expands to                                                                          |
| ---------- | ----------------------------------------------------------------------------------- |
| `timeline` | Template for a `timeline` block (includes `title`, `unit`, `range`, and `calendar`) |
| `lane`     | Template for a `lane` definition (includes `kind` and `order`)                      |
| `span`     | Template for a `span` event (with start and end dates)                              |

Snippets are active in files with the `.tdsl` extension.

The VS Code extension provides syntax highlighting and snippets. Interactive features such as diagnostics, completion, hover, and goto-definition are handled by the language server (LSP) described below.

## Language Server (LSP)

Since v1.11.0, you can start the Timeline DSL language server (LSP) with `tdsl lsp`. The server communicates over standard input/output (stdio), so any editor with an LSP client (VS Code, Neovim, Helix, Emacs, etc.) can connect to it and get interactive assistance while editing `.tdsl` files. It is independent of the VS Code extension (which only provides syntax highlighting and snippets).

| Feature         | Description                                                      | Version  |
| --------------- | ---------------------------------------------------------------- | -------- |
| Diagnostics     | Reports syntax and semantic errors in real time while editing    | v1.11.0+ |
| Completion      | Completes DSL keywords                                           | v1.12.0+ |
| Hover           | Shows hover information for lane IDs / QIDs                      | v1.12.0+ |
| Goto Definition | Jumps from a lane reference to its lane declaration              | v1.12.0+ |
| References      | Returns the declaration and all reference locations of a lane ID | v1.13.0+ |
| Rename          | Renames a lane ID across its declaration and all references      | v1.13.0+ |
| Code Action     | Provides lint auto-fixes as quick fixes                          | v1.13.0+ |
| Document Symbol | Returns hierarchical symbols (timeline > lane > items)           | v1.13.0+ |

The launch command takes no arguments.

```sh
tdsl lsp
```

In each editor, register the command `tdsl lsp` as the language server bound to `.tdsl` (language ID: `tdsl`). Example for Neovim (core LSP from 0.10+, no plugin required):

```lua
vim.filetype.add({ extension = { tdsl = "tdsl" } })

vim.api.nvim_create_autocmd("FileType", {
  pattern = "tdsl",
  callback = function()
    vim.lsp.start({
      name = "tdsl-lsp",
      cmd = { "tdsl", "lsp" },
      root_dir = vim.fs.root(0, { ".git" }),
    })
  end,
})
```

See also [Commands](/en/docs/commands/#lsp) for an overview of `tdsl lsp`.
